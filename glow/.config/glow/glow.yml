# style name or JSON path (default "auto")
# ... unable to use $HOME or ~ in style configuration! (WTF)
style: "/Users/askarby/.config/glow/themes/catppuccin-macchiato.json"

# show local files only; no network (TUI-mode only)
local: false

# mouse support (TUI-mode only)
mouse: false

# use pager to display markdown
pager: true

# word-wrap at width
width: 120